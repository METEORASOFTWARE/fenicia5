import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'fenicia2',
  webDir: 'dist/fenicia2',
  bundledWebRuntime: false
};

export default config;
